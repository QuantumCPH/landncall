<?php

/**
 * Base class that represents a row from the 'product' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * 01/13/12 05:16:51
 *
 * @package    lib.model.om
 */
abstract class BaseProduct extends BaseObject  implements Persistent {


  const PEER = 'ProductPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ProductPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the price field.
	 * Note: this column has a database default value of: 0
	 * @var        double
	 */
	protected $price;

	/**
	 * The value for the description field.
	 * @var        string
	 */
	protected $description;

	/**
	 * The value for the initial_balance field.
	 * Note: this column has a database default value of: 0
	 * @var        double
	 */
	protected $initial_balance;

	/**
	 * The value for the created_at field.
	 * Note: this column has a database default value of: (expression) CURRENT_TIMESTAMP
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * The value for the is_extra_fill_applied field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_extra_fill_applied;

	/**
	 * The value for the include_in_zerocall field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $include_in_zerocall;

	/**
	 * The value for the is_in_store field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_in_store;

	/**
	 * The value for the sms_code field.
	 * @var        string
	 */
	protected $sms_code;

	/**
	 * The value for the country field.
	 * @var        int
	 */
	protected $country;

	/**
	 * The value for the refill field.
	 * @var        string
	 */
	protected $refill;

	/**
	 * The value for the country_id field.
	 * @var        int
	 */
	protected $country_id;

	/**
	 * The value for the refill_options field.
	 * @var        string
	 */
	protected $refill_options;

	/**
	 * The value for the product_order field.
	 * @var        int
	 */
	protected $product_order;

	/**
	 * The value for the product_type_package field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $product_type_package;

	/**
	 * The value for the product_country_us field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $product_country_us;

	/**
	 * @var        EnableCountry
	 */
	protected $aEnableCountry;

	/**
	 * @var        array AgentProduct[] Collection to store aggregation of AgentProduct objects.
	 */
	protected $collAgentProducts;

	/**
	 * @var        Criteria The criteria used to select the current contents of collAgentProducts.
	 */
	private $lastAgentProductCriteria = null;

	/**
	 * @var        array CustomerOrder[] Collection to store aggregation of CustomerOrder objects.
	 */
	protected $collCustomerOrders;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCustomerOrders.
	 */
	private $lastCustomerOrderCriteria = null;

	/**
	 * @var        array CustomerProduct[] Collection to store aggregation of CustomerProduct objects.
	 */
	protected $collCustomerProducts;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCustomerProducts.
	 */
	private $lastCustomerProductCriteria = null;

	/**
	 * @var        array EmployeeProduct[] Collection to store aggregation of EmployeeProduct objects.
	 */
	protected $collEmployeeProducts;

	/**
	 * @var        Criteria The criteria used to select the current contents of collEmployeeProducts.
	 */
	private $lastEmployeeProductCriteria = null;

	/**
	 * @var        array ProductOrder[] Collection to store aggregation of ProductOrder objects.
	 */
	protected $collProductOrders;

	/**
	 * @var        Criteria The criteria used to select the current contents of collProductOrders.
	 */
	private $lastProductOrderCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseProduct object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->price = 0;
		$this->initial_balance = 0;
		$this->is_extra_fill_applied = false;
		$this->include_in_zerocall = false;
		$this->is_in_store = false;
		$this->product_type_package = false;
		$this->product_country_us = false;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [price] column value.
	 * 
	 * @return     double
	 */
	public function getPrice()
	{
		return $this->price;
	}

	/**
	 * Get the [description] column value.
	 * 
	 * @return     string
	 */
	public function getDescription()
	{
		return $this->description;
	}

	/**
	 * Get the [initial_balance] column value.
	 * 
	 * @return     double
	 */
	public function getInitialBalance()
	{
		return $this->initial_balance;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [is_extra_fill_applied] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsExtraFillApplied()
	{
		return $this->is_extra_fill_applied;
	}

	/**
	 * Get the [include_in_zerocall] column value.
	 * 
	 * @return     boolean
	 */
	public function getIncludeInZerocall()
	{
		return $this->include_in_zerocall;
	}

	/**
	 * Get the [is_in_store] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsInStore()
	{
		return $this->is_in_store;
	}

	/**
	 * Get the [sms_code] column value.
	 * 
	 * @return     string
	 */
	public function getSmsCode()
	{
		return $this->sms_code;
	}

	/**
	 * Get the [country] column value.
	 * 
	 * @return     int
	 */
	public function getCountry()
	{
		return $this->country;
	}

	/**
	 * Get the [refill] column value.
	 * 
	 * @return     string
	 */
	public function getRefill()
	{
		return $this->refill;
	}

	/**
	 * Get the [country_id] column value.
	 * 
	 * @return     int
	 */
	public function getCountryId()
	{
		return $this->country_id;
	}

	/**
	 * Get the [refill_options] column value.
	 * 
	 * @return     string
	 */
	public function getRefillOptions()
	{
		return $this->refill_options;
	}

	/**
	 * Get the [product_order] column value.
	 * 
	 * @return     int
	 */
	public function getProductOrder()
	{
		return $this->product_order;
	}

	/**
	 * Get the [product_type_package] column value.
	 * 
	 * @return     boolean
	 */
	public function getProductTypePackage()
	{
		return $this->product_type_package;
	}

	/**
	 * Get the [product_country_us] column value.
	 * 
	 * @return     boolean
	 */
	public function getProductCountryUs()
	{
		return $this->product_country_us;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = ProductPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = ProductPeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Set the value of [price] column.
	 * 
	 * @param      double $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setPrice($v)
	{
		if ($v !== null) {
			$v = (double) $v;
		}

		if ($this->price !== $v || $v === 0) {
			$this->price = $v;
			$this->modifiedColumns[] = ProductPeer::PRICE;
		}

		return $this;
	} // setPrice()

	/**
	 * Set the value of [description] column.
	 * 
	 * @param      string $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setDescription($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->description !== $v) {
			$this->description = $v;
			$this->modifiedColumns[] = ProductPeer::DESCRIPTION;
		}

		return $this;
	} // setDescription()

	/**
	 * Set the value of [initial_balance] column.
	 * 
	 * @param      double $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setInitialBalance($v)
	{
		if ($v !== null) {
			$v = (double) $v;
		}

		if ($this->initial_balance !== $v || $v === 0) {
			$this->initial_balance = $v;
			$this->modifiedColumns[] = ProductPeer::INITIAL_BALANCE;
		}

		return $this;
	} // setInitialBalance()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Product The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ProductPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Product The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ProductPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Set the value of [is_extra_fill_applied] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setIsExtraFillApplied($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_extra_fill_applied !== $v || $v === false) {
			$this->is_extra_fill_applied = $v;
			$this->modifiedColumns[] = ProductPeer::IS_EXTRA_FILL_APPLIED;
		}

		return $this;
	} // setIsExtraFillApplied()

	/**
	 * Set the value of [include_in_zerocall] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setIncludeInZerocall($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->include_in_zerocall !== $v || $v === false) {
			$this->include_in_zerocall = $v;
			$this->modifiedColumns[] = ProductPeer::INCLUDE_IN_ZEROCALL;
		}

		return $this;
	} // setIncludeInZerocall()

	/**
	 * Set the value of [is_in_store] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setIsInStore($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_in_store !== $v || $v === false) {
			$this->is_in_store = $v;
			$this->modifiedColumns[] = ProductPeer::IS_IN_STORE;
		}

		return $this;
	} // setIsInStore()

	/**
	 * Set the value of [sms_code] column.
	 * 
	 * @param      string $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setSmsCode($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->sms_code !== $v) {
			$this->sms_code = $v;
			$this->modifiedColumns[] = ProductPeer::SMS_CODE;
		}

		return $this;
	} // setSmsCode()

	/**
	 * Set the value of [country] column.
	 * 
	 * @param      int $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setCountry($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->country !== $v) {
			$this->country = $v;
			$this->modifiedColumns[] = ProductPeer::COUNTRY;
		}

		return $this;
	} // setCountry()

	/**
	 * Set the value of [refill] column.
	 * 
	 * @param      string $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setRefill($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->refill !== $v) {
			$this->refill = $v;
			$this->modifiedColumns[] = ProductPeer::REFILL;
		}

		return $this;
	} // setRefill()

	/**
	 * Set the value of [country_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setCountryId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->country_id !== $v) {
			$this->country_id = $v;
			$this->modifiedColumns[] = ProductPeer::COUNTRY_ID;
		}

		if ($this->aEnableCountry !== null && $this->aEnableCountry->getId() !== $v) {
			$this->aEnableCountry = null;
		}

		return $this;
	} // setCountryId()

	/**
	 * Set the value of [refill_options] column.
	 * 
	 * @param      string $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setRefillOptions($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->refill_options !== $v) {
			$this->refill_options = $v;
			$this->modifiedColumns[] = ProductPeer::REFILL_OPTIONS;
		}

		return $this;
	} // setRefillOptions()

	/**
	 * Set the value of [product_order] column.
	 * 
	 * @param      int $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setProductOrder($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->product_order !== $v) {
			$this->product_order = $v;
			$this->modifiedColumns[] = ProductPeer::PRODUCT_ORDER;
		}

		return $this;
	} // setProductOrder()

	/**
	 * Set the value of [product_type_package] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setProductTypePackage($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->product_type_package !== $v || $v === false) {
			$this->product_type_package = $v;
			$this->modifiedColumns[] = ProductPeer::PRODUCT_TYPE_PACKAGE;
		}

		return $this;
	} // setProductTypePackage()

	/**
	 * Set the value of [product_country_us] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Product The current object (for fluent API support)
	 */
	public function setProductCountryUs($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->product_country_us !== $v || $v === false) {
			$this->product_country_us = $v;
			$this->modifiedColumns[] = ProductPeer::PRODUCT_COUNTRY_US;
		}

		return $this;
	} // setProductCountryUs()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(ProductPeer::PRICE,ProductPeer::INITIAL_BALANCE,ProductPeer::IS_EXTRA_FILL_APPLIED,ProductPeer::INCLUDE_IN_ZEROCALL,ProductPeer::IS_IN_STORE,ProductPeer::PRODUCT_TYPE_PACKAGE,ProductPeer::PRODUCT_COUNTRY_US))) {
				return false;
			}

			if ($this->price !== 0) {
				return false;
			}

			if ($this->initial_balance !== 0) {
				return false;
			}

			if ($this->is_extra_fill_applied !== false) {
				return false;
			}

			if ($this->include_in_zerocall !== false) {
				return false;
			}

			if ($this->is_in_store !== false) {
				return false;
			}

			if ($this->product_type_package !== false) {
				return false;
			}

			if ($this->product_country_us !== false) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->name = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->price = ($row[$startcol + 2] !== null) ? (double) $row[$startcol + 2] : null;
			$this->description = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->initial_balance = ($row[$startcol + 4] !== null) ? (double) $row[$startcol + 4] : null;
			$this->created_at = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->updated_at = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->is_extra_fill_applied = ($row[$startcol + 7] !== null) ? (boolean) $row[$startcol + 7] : null;
			$this->include_in_zerocall = ($row[$startcol + 8] !== null) ? (boolean) $row[$startcol + 8] : null;
			$this->is_in_store = ($row[$startcol + 9] !== null) ? (boolean) $row[$startcol + 9] : null;
			$this->sms_code = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->country = ($row[$startcol + 11] !== null) ? (int) $row[$startcol + 11] : null;
			$this->refill = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->country_id = ($row[$startcol + 13] !== null) ? (int) $row[$startcol + 13] : null;
			$this->refill_options = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
			$this->product_order = ($row[$startcol + 15] !== null) ? (int) $row[$startcol + 15] : null;
			$this->product_type_package = ($row[$startcol + 16] !== null) ? (boolean) $row[$startcol + 16] : null;
			$this->product_country_us = ($row[$startcol + 17] !== null) ? (boolean) $row[$startcol + 17] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 18; // 18 = ProductPeer::NUM_COLUMNS - ProductPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Product object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aEnableCountry !== null && $this->country_id !== $this->aEnableCountry->getId()) {
			$this->aEnableCountry = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ProductPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = ProductPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aEnableCountry = null;
			$this->collAgentProducts = null;
			$this->lastAgentProductCriteria = null;

			$this->collCustomerOrders = null;
			$this->lastCustomerOrderCriteria = null;

			$this->collCustomerProducts = null;
			$this->lastCustomerProductCriteria = null;

			$this->collEmployeeProducts = null;
			$this->lastEmployeeProductCriteria = null;

			$this->collProductOrders = null;
			$this->lastProductOrderCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseProduct:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ProductPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			ProductPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseProduct:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseProduct:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(ProductPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

    if ($this->isModified() && !$this->isColumnModified(ProductPeer::UPDATED_AT))
    {
      $this->setUpdatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ProductPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseProduct:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			ProductPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aEnableCountry !== null) {
				if ($this->aEnableCountry->isModified() || $this->aEnableCountry->isNew()) {
					$affectedRows += $this->aEnableCountry->save($con);
				}
				$this->setEnableCountry($this->aEnableCountry);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = ProductPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ProductPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += ProductPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collAgentProducts !== null) {
				foreach ($this->collAgentProducts as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCustomerOrders !== null) {
				foreach ($this->collCustomerOrders as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCustomerProducts !== null) {
				foreach ($this->collCustomerProducts as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collEmployeeProducts !== null) {
				foreach ($this->collEmployeeProducts as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collProductOrders !== null) {
				foreach ($this->collProductOrders as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aEnableCountry !== null) {
				if (!$this->aEnableCountry->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aEnableCountry->getValidationFailures());
				}
			}


			if (($retval = ProductPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collAgentProducts !== null) {
					foreach ($this->collAgentProducts as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCustomerOrders !== null) {
					foreach ($this->collCustomerOrders as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCustomerProducts !== null) {
					foreach ($this->collCustomerProducts as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collEmployeeProducts !== null) {
					foreach ($this->collEmployeeProducts as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collProductOrders !== null) {
					foreach ($this->collProductOrders as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ProductPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getName();
				break;
			case 2:
				return $this->getPrice();
				break;
			case 3:
				return $this->getDescription();
				break;
			case 4:
				return $this->getInitialBalance();
				break;
			case 5:
				return $this->getCreatedAt();
				break;
			case 6:
				return $this->getUpdatedAt();
				break;
			case 7:
				return $this->getIsExtraFillApplied();
				break;
			case 8:
				return $this->getIncludeInZerocall();
				break;
			case 9:
				return $this->getIsInStore();
				break;
			case 10:
				return $this->getSmsCode();
				break;
			case 11:
				return $this->getCountry();
				break;
			case 12:
				return $this->getRefill();
				break;
			case 13:
				return $this->getCountryId();
				break;
			case 14:
				return $this->getRefillOptions();
				break;
			case 15:
				return $this->getProductOrder();
				break;
			case 16:
				return $this->getProductTypePackage();
				break;
			case 17:
				return $this->getProductCountryUs();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = ProductPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getName(),
			$keys[2] => $this->getPrice(),
			$keys[3] => $this->getDescription(),
			$keys[4] => $this->getInitialBalance(),
			$keys[5] => $this->getCreatedAt(),
			$keys[6] => $this->getUpdatedAt(),
			$keys[7] => $this->getIsExtraFillApplied(),
			$keys[8] => $this->getIncludeInZerocall(),
			$keys[9] => $this->getIsInStore(),
			$keys[10] => $this->getSmsCode(),
			$keys[11] => $this->getCountry(),
			$keys[12] => $this->getRefill(),
			$keys[13] => $this->getCountryId(),
			$keys[14] => $this->getRefillOptions(),
			$keys[15] => $this->getProductOrder(),
			$keys[16] => $this->getProductTypePackage(),
			$keys[17] => $this->getProductCountryUs(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ProductPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setName($value);
				break;
			case 2:
				$this->setPrice($value);
				break;
			case 3:
				$this->setDescription($value);
				break;
			case 4:
				$this->setInitialBalance($value);
				break;
			case 5:
				$this->setCreatedAt($value);
				break;
			case 6:
				$this->setUpdatedAt($value);
				break;
			case 7:
				$this->setIsExtraFillApplied($value);
				break;
			case 8:
				$this->setIncludeInZerocall($value);
				break;
			case 9:
				$this->setIsInStore($value);
				break;
			case 10:
				$this->setSmsCode($value);
				break;
			case 11:
				$this->setCountry($value);
				break;
			case 12:
				$this->setRefill($value);
				break;
			case 13:
				$this->setCountryId($value);
				break;
			case 14:
				$this->setRefillOptions($value);
				break;
			case 15:
				$this->setProductOrder($value);
				break;
			case 16:
				$this->setProductTypePackage($value);
				break;
			case 17:
				$this->setProductCountryUs($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = ProductPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setName($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setPrice($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setDescription($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setInitialBalance($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setCreatedAt($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setUpdatedAt($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setIsExtraFillApplied($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setIncludeInZerocall($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setIsInStore($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setSmsCode($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setCountry($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setRefill($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setCountryId($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setRefillOptions($arr[$keys[14]]);
		if (array_key_exists($keys[15], $arr)) $this->setProductOrder($arr[$keys[15]]);
		if (array_key_exists($keys[16], $arr)) $this->setProductTypePackage($arr[$keys[16]]);
		if (array_key_exists($keys[17], $arr)) $this->setProductCountryUs($arr[$keys[17]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ProductPeer::DATABASE_NAME);

		if ($this->isColumnModified(ProductPeer::ID)) $criteria->add(ProductPeer::ID, $this->id);
		if ($this->isColumnModified(ProductPeer::NAME)) $criteria->add(ProductPeer::NAME, $this->name);
		if ($this->isColumnModified(ProductPeer::PRICE)) $criteria->add(ProductPeer::PRICE, $this->price);
		if ($this->isColumnModified(ProductPeer::DESCRIPTION)) $criteria->add(ProductPeer::DESCRIPTION, $this->description);
		if ($this->isColumnModified(ProductPeer::INITIAL_BALANCE)) $criteria->add(ProductPeer::INITIAL_BALANCE, $this->initial_balance);
		if ($this->isColumnModified(ProductPeer::CREATED_AT)) $criteria->add(ProductPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(ProductPeer::UPDATED_AT)) $criteria->add(ProductPeer::UPDATED_AT, $this->updated_at);
		if ($this->isColumnModified(ProductPeer::IS_EXTRA_FILL_APPLIED)) $criteria->add(ProductPeer::IS_EXTRA_FILL_APPLIED, $this->is_extra_fill_applied);
		if ($this->isColumnModified(ProductPeer::INCLUDE_IN_ZEROCALL)) $criteria->add(ProductPeer::INCLUDE_IN_ZEROCALL, $this->include_in_zerocall);
		if ($this->isColumnModified(ProductPeer::IS_IN_STORE)) $criteria->add(ProductPeer::IS_IN_STORE, $this->is_in_store);
		if ($this->isColumnModified(ProductPeer::SMS_CODE)) $criteria->add(ProductPeer::SMS_CODE, $this->sms_code);
		if ($this->isColumnModified(ProductPeer::COUNTRY)) $criteria->add(ProductPeer::COUNTRY, $this->country);
		if ($this->isColumnModified(ProductPeer::REFILL)) $criteria->add(ProductPeer::REFILL, $this->refill);
		if ($this->isColumnModified(ProductPeer::COUNTRY_ID)) $criteria->add(ProductPeer::COUNTRY_ID, $this->country_id);
		if ($this->isColumnModified(ProductPeer::REFILL_OPTIONS)) $criteria->add(ProductPeer::REFILL_OPTIONS, $this->refill_options);
		if ($this->isColumnModified(ProductPeer::PRODUCT_ORDER)) $criteria->add(ProductPeer::PRODUCT_ORDER, $this->product_order);
		if ($this->isColumnModified(ProductPeer::PRODUCT_TYPE_PACKAGE)) $criteria->add(ProductPeer::PRODUCT_TYPE_PACKAGE, $this->product_type_package);
		if ($this->isColumnModified(ProductPeer::PRODUCT_COUNTRY_US)) $criteria->add(ProductPeer::PRODUCT_COUNTRY_US, $this->product_country_us);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ProductPeer::DATABASE_NAME);

		$criteria->add(ProductPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Product (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setName($this->name);

		$copyObj->setPrice($this->price);

		$copyObj->setDescription($this->description);

		$copyObj->setInitialBalance($this->initial_balance);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);

		$copyObj->setIsExtraFillApplied($this->is_extra_fill_applied);

		$copyObj->setIncludeInZerocall($this->include_in_zerocall);

		$copyObj->setIsInStore($this->is_in_store);

		$copyObj->setSmsCode($this->sms_code);

		$copyObj->setCountry($this->country);

		$copyObj->setRefill($this->refill);

		$copyObj->setCountryId($this->country_id);

		$copyObj->setRefillOptions($this->refill_options);

		$copyObj->setProductOrder($this->product_order);

		$copyObj->setProductTypePackage($this->product_type_package);

		$copyObj->setProductCountryUs($this->product_country_us);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getAgentProducts() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addAgentProduct($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCustomerOrders() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCustomerOrder($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCustomerProducts() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCustomerProduct($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getEmployeeProducts() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addEmployeeProduct($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getProductOrders() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addProductOrder($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Product Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ProductPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ProductPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a EnableCountry object.
	 *
	 * @param      EnableCountry $v
	 * @return     Product The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setEnableCountry(EnableCountry $v = null)
	{
		if ($v === null) {
			$this->setCountryId(NULL);
		} else {
			$this->setCountryId($v->getId());
		}

		$this->aEnableCountry = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the EnableCountry object, it will not be re-added.
		if ($v !== null) {
			$v->addProduct($this);
		}

		return $this;
	}


	/**
	 * Get the associated EnableCountry object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     EnableCountry The associated EnableCountry object.
	 * @throws     PropelException
	 */
	public function getEnableCountry(PropelPDO $con = null)
	{
		if ($this->aEnableCountry === null && ($this->country_id !== null)) {
			$c = new Criteria(EnableCountryPeer::DATABASE_NAME);
			$c->add(EnableCountryPeer::ID, $this->country_id);
			$this->aEnableCountry = EnableCountryPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aEnableCountry->addProducts($this);
			 */
		}
		return $this->aEnableCountry;
	}

	/**
	 * Clears out the collAgentProducts collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addAgentProducts()
	 */
	public function clearAgentProducts()
	{
		$this->collAgentProducts = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collAgentProducts collection (array).
	 *
	 * By default this just sets the collAgentProducts collection to an empty array (like clearcollAgentProducts());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initAgentProducts()
	{
		$this->collAgentProducts = array();
	}

	/**
	 * Gets an array of AgentProduct objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Product has previously been saved, it will retrieve
	 * related AgentProducts from storage. If this Product is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array AgentProduct[]
	 * @throws     PropelException
	 */
	public function getAgentProducts($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentProducts === null) {
			if ($this->isNew()) {
			   $this->collAgentProducts = array();
			} else {

				$criteria->add(AgentProductPeer::PRODUCT_ID, $this->id);

				AgentProductPeer::addSelectColumns($criteria);
				$this->collAgentProducts = AgentProductPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AgentProductPeer::PRODUCT_ID, $this->id);

				AgentProductPeer::addSelectColumns($criteria);
				if (!isset($this->lastAgentProductCriteria) || !$this->lastAgentProductCriteria->equals($criteria)) {
					$this->collAgentProducts = AgentProductPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAgentProductCriteria = $criteria;
		return $this->collAgentProducts;
	}

	/**
	 * Returns the number of related AgentProduct objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related AgentProduct objects.
	 * @throws     PropelException
	 */
	public function countAgentProducts(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collAgentProducts === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(AgentProductPeer::PRODUCT_ID, $this->id);

				$count = AgentProductPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(AgentProductPeer::PRODUCT_ID, $this->id);

				if (!isset($this->lastAgentProductCriteria) || !$this->lastAgentProductCriteria->equals($criteria)) {
					$count = AgentProductPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collAgentProducts);
				}
			} else {
				$count = count($this->collAgentProducts);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a AgentProduct object to this object
	 * through the AgentProduct foreign key attribute.
	 *
	 * @param      AgentProduct $l AgentProduct
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAgentProduct(AgentProduct $l)
	{
		if ($this->collAgentProducts === null) {
			$this->initAgentProducts();
		}
		if (!in_array($l, $this->collAgentProducts, true)) { // only add it if the **same** object is not already associated
			array_push($this->collAgentProducts, $l);
			$l->setProduct($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Product is new, it will return
	 * an empty collection; or if this Product has previously
	 * been saved, it will retrieve related AgentProducts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Product.
	 */
	public function getAgentProductsJoinAgentCompany($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentProducts === null) {
			if ($this->isNew()) {
				$this->collAgentProducts = array();
			} else {

				$criteria->add(AgentProductPeer::PRODUCT_ID, $this->id);

				$this->collAgentProducts = AgentProductPeer::doSelectJoinAgentCompany($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentProductPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastAgentProductCriteria) || !$this->lastAgentProductCriteria->equals($criteria)) {
				$this->collAgentProducts = AgentProductPeer::doSelectJoinAgentCompany($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentProductCriteria = $criteria;

		return $this->collAgentProducts;
	}

	/**
	 * Clears out the collCustomerOrders collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCustomerOrders()
	 */
	public function clearCustomerOrders()
	{
		$this->collCustomerOrders = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCustomerOrders collection (array).
	 *
	 * By default this just sets the collCustomerOrders collection to an empty array (like clearcollCustomerOrders());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCustomerOrders()
	{
		$this->collCustomerOrders = array();
	}

	/**
	 * Gets an array of CustomerOrder objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Product has previously been saved, it will retrieve
	 * related CustomerOrders from storage. If this Product is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CustomerOrder[]
	 * @throws     PropelException
	 */
	public function getCustomerOrders($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCustomerOrders === null) {
			if ($this->isNew()) {
			   $this->collCustomerOrders = array();
			} else {

				$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

				CustomerOrderPeer::addSelectColumns($criteria);
				$this->collCustomerOrders = CustomerOrderPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

				CustomerOrderPeer::addSelectColumns($criteria);
				if (!isset($this->lastCustomerOrderCriteria) || !$this->lastCustomerOrderCriteria->equals($criteria)) {
					$this->collCustomerOrders = CustomerOrderPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCustomerOrderCriteria = $criteria;
		return $this->collCustomerOrders;
	}

	/**
	 * Returns the number of related CustomerOrder objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CustomerOrder objects.
	 * @throws     PropelException
	 */
	public function countCustomerOrders(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCustomerOrders === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

				$count = CustomerOrderPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

				if (!isset($this->lastCustomerOrderCriteria) || !$this->lastCustomerOrderCriteria->equals($criteria)) {
					$count = CustomerOrderPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCustomerOrders);
				}
			} else {
				$count = count($this->collCustomerOrders);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CustomerOrder object to this object
	 * through the CustomerOrder foreign key attribute.
	 *
	 * @param      CustomerOrder $l CustomerOrder
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCustomerOrder(CustomerOrder $l)
	{
		if ($this->collCustomerOrders === null) {
			$this->initCustomerOrders();
		}
		if (!in_array($l, $this->collCustomerOrders, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCustomerOrders, $l);
			$l->setProduct($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Product is new, it will return
	 * an empty collection; or if this Product has previously
	 * been saved, it will retrieve related CustomerOrders from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Product.
	 */
	public function getCustomerOrdersJoinEntityStatus($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCustomerOrders === null) {
			if ($this->isNew()) {
				$this->collCustomerOrders = array();
			} else {

				$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

				$this->collCustomerOrders = CustomerOrderPeer::doSelectJoinEntityStatus($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastCustomerOrderCriteria) || !$this->lastCustomerOrderCriteria->equals($criteria)) {
				$this->collCustomerOrders = CustomerOrderPeer::doSelectJoinEntityStatus($criteria, $con, $join_behavior);
			}
		}
		$this->lastCustomerOrderCriteria = $criteria;

		return $this->collCustomerOrders;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Product is new, it will return
	 * an empty collection; or if this Product has previously
	 * been saved, it will retrieve related CustomerOrders from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Product.
	 */
	public function getCustomerOrdersJoinCustomer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCustomerOrders === null) {
			if ($this->isNew()) {
				$this->collCustomerOrders = array();
			} else {

				$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

				$this->collCustomerOrders = CustomerOrderPeer::doSelectJoinCustomer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastCustomerOrderCriteria) || !$this->lastCustomerOrderCriteria->equals($criteria)) {
				$this->collCustomerOrders = CustomerOrderPeer::doSelectJoinCustomer($criteria, $con, $join_behavior);
			}
		}
		$this->lastCustomerOrderCriteria = $criteria;

		return $this->collCustomerOrders;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Product is new, it will return
	 * an empty collection; or if this Product has previously
	 * been saved, it will retrieve related CustomerOrders from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Product.
	 */
	public function getCustomerOrdersJoinAgentCommissionPackage($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCustomerOrders === null) {
			if ($this->isNew()) {
				$this->collCustomerOrders = array();
			} else {

				$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

				$this->collCustomerOrders = CustomerOrderPeer::doSelectJoinAgentCommissionPackage($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CustomerOrderPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastCustomerOrderCriteria) || !$this->lastCustomerOrderCriteria->equals($criteria)) {
				$this->collCustomerOrders = CustomerOrderPeer::doSelectJoinAgentCommissionPackage($criteria, $con, $join_behavior);
			}
		}
		$this->lastCustomerOrderCriteria = $criteria;

		return $this->collCustomerOrders;
	}

	/**
	 * Clears out the collCustomerProducts collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCustomerProducts()
	 */
	public function clearCustomerProducts()
	{
		$this->collCustomerProducts = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCustomerProducts collection (array).
	 *
	 * By default this just sets the collCustomerProducts collection to an empty array (like clearcollCustomerProducts());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCustomerProducts()
	{
		$this->collCustomerProducts = array();
	}

	/**
	 * Gets an array of CustomerProduct objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Product has previously been saved, it will retrieve
	 * related CustomerProducts from storage. If this Product is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CustomerProduct[]
	 * @throws     PropelException
	 */
	public function getCustomerProducts($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCustomerProducts === null) {
			if ($this->isNew()) {
			   $this->collCustomerProducts = array();
			} else {

				$criteria->add(CustomerProductPeer::PRODUCT_ID, $this->id);

				CustomerProductPeer::addSelectColumns($criteria);
				$this->collCustomerProducts = CustomerProductPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CustomerProductPeer::PRODUCT_ID, $this->id);

				CustomerProductPeer::addSelectColumns($criteria);
				if (!isset($this->lastCustomerProductCriteria) || !$this->lastCustomerProductCriteria->equals($criteria)) {
					$this->collCustomerProducts = CustomerProductPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCustomerProductCriteria = $criteria;
		return $this->collCustomerProducts;
	}

	/**
	 * Returns the number of related CustomerProduct objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CustomerProduct objects.
	 * @throws     PropelException
	 */
	public function countCustomerProducts(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCustomerProducts === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CustomerProductPeer::PRODUCT_ID, $this->id);

				$count = CustomerProductPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CustomerProductPeer::PRODUCT_ID, $this->id);

				if (!isset($this->lastCustomerProductCriteria) || !$this->lastCustomerProductCriteria->equals($criteria)) {
					$count = CustomerProductPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCustomerProducts);
				}
			} else {
				$count = count($this->collCustomerProducts);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CustomerProduct object to this object
	 * through the CustomerProduct foreign key attribute.
	 *
	 * @param      CustomerProduct $l CustomerProduct
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCustomerProduct(CustomerProduct $l)
	{
		if ($this->collCustomerProducts === null) {
			$this->initCustomerProducts();
		}
		if (!in_array($l, $this->collCustomerProducts, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCustomerProducts, $l);
			$l->setProduct($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Product is new, it will return
	 * an empty collection; or if this Product has previously
	 * been saved, it will retrieve related CustomerProducts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Product.
	 */
	public function getCustomerProductsJoinCustomer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCustomerProducts === null) {
			if ($this->isNew()) {
				$this->collCustomerProducts = array();
			} else {

				$criteria->add(CustomerProductPeer::PRODUCT_ID, $this->id);

				$this->collCustomerProducts = CustomerProductPeer::doSelectJoinCustomer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CustomerProductPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastCustomerProductCriteria) || !$this->lastCustomerProductCriteria->equals($criteria)) {
				$this->collCustomerProducts = CustomerProductPeer::doSelectJoinCustomer($criteria, $con, $join_behavior);
			}
		}
		$this->lastCustomerProductCriteria = $criteria;

		return $this->collCustomerProducts;
	}

	/**
	 * Clears out the collEmployeeProducts collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addEmployeeProducts()
	 */
	public function clearEmployeeProducts()
	{
		$this->collEmployeeProducts = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collEmployeeProducts collection (array).
	 *
	 * By default this just sets the collEmployeeProducts collection to an empty array (like clearcollEmployeeProducts());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initEmployeeProducts()
	{
		$this->collEmployeeProducts = array();
	}

	/**
	 * Gets an array of EmployeeProduct objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Product has previously been saved, it will retrieve
	 * related EmployeeProducts from storage. If this Product is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array EmployeeProduct[]
	 * @throws     PropelException
	 */
	public function getEmployeeProducts($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEmployeeProducts === null) {
			if ($this->isNew()) {
			   $this->collEmployeeProducts = array();
			} else {

				$criteria->add(EmployeeProductPeer::PRODUCT_ID, $this->id);

				EmployeeProductPeer::addSelectColumns($criteria);
				$this->collEmployeeProducts = EmployeeProductPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(EmployeeProductPeer::PRODUCT_ID, $this->id);

				EmployeeProductPeer::addSelectColumns($criteria);
				if (!isset($this->lastEmployeeProductCriteria) || !$this->lastEmployeeProductCriteria->equals($criteria)) {
					$this->collEmployeeProducts = EmployeeProductPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastEmployeeProductCriteria = $criteria;
		return $this->collEmployeeProducts;
	}

	/**
	 * Returns the number of related EmployeeProduct objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related EmployeeProduct objects.
	 * @throws     PropelException
	 */
	public function countEmployeeProducts(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collEmployeeProducts === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(EmployeeProductPeer::PRODUCT_ID, $this->id);

				$count = EmployeeProductPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(EmployeeProductPeer::PRODUCT_ID, $this->id);

				if (!isset($this->lastEmployeeProductCriteria) || !$this->lastEmployeeProductCriteria->equals($criteria)) {
					$count = EmployeeProductPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collEmployeeProducts);
				}
			} else {
				$count = count($this->collEmployeeProducts);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a EmployeeProduct object to this object
	 * through the EmployeeProduct foreign key attribute.
	 *
	 * @param      EmployeeProduct $l EmployeeProduct
	 * @return     void
	 * @throws     PropelException
	 */
	public function addEmployeeProduct(EmployeeProduct $l)
	{
		if ($this->collEmployeeProducts === null) {
			$this->initEmployeeProducts();
		}
		if (!in_array($l, $this->collEmployeeProducts, true)) { // only add it if the **same** object is not already associated
			array_push($this->collEmployeeProducts, $l);
			$l->setProduct($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Product is new, it will return
	 * an empty collection; or if this Product has previously
	 * been saved, it will retrieve related EmployeeProducts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Product.
	 */
	public function getEmployeeProductsJoinEmployee($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEmployeeProducts === null) {
			if ($this->isNew()) {
				$this->collEmployeeProducts = array();
			} else {

				$criteria->add(EmployeeProductPeer::PRODUCT_ID, $this->id);

				$this->collEmployeeProducts = EmployeeProductPeer::doSelectJoinEmployee($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(EmployeeProductPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastEmployeeProductCriteria) || !$this->lastEmployeeProductCriteria->equals($criteria)) {
				$this->collEmployeeProducts = EmployeeProductPeer::doSelectJoinEmployee($criteria, $con, $join_behavior);
			}
		}
		$this->lastEmployeeProductCriteria = $criteria;

		return $this->collEmployeeProducts;
	}

	/**
	 * Clears out the collProductOrders collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addProductOrders()
	 */
	public function clearProductOrders()
	{
		$this->collProductOrders = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collProductOrders collection (array).
	 *
	 * By default this just sets the collProductOrders collection to an empty array (like clearcollProductOrders());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initProductOrders()
	{
		$this->collProductOrders = array();
	}

	/**
	 * Gets an array of ProductOrder objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Product has previously been saved, it will retrieve
	 * related ProductOrders from storage. If this Product is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ProductOrder[]
	 * @throws     PropelException
	 */
	public function getProductOrders($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collProductOrders === null) {
			if ($this->isNew()) {
			   $this->collProductOrders = array();
			} else {

				$criteria->add(ProductOrderPeer::PRODUCT_ID, $this->id);

				ProductOrderPeer::addSelectColumns($criteria);
				$this->collProductOrders = ProductOrderPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ProductOrderPeer::PRODUCT_ID, $this->id);

				ProductOrderPeer::addSelectColumns($criteria);
				if (!isset($this->lastProductOrderCriteria) || !$this->lastProductOrderCriteria->equals($criteria)) {
					$this->collProductOrders = ProductOrderPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastProductOrderCriteria = $criteria;
		return $this->collProductOrders;
	}

	/**
	 * Returns the number of related ProductOrder objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ProductOrder objects.
	 * @throws     PropelException
	 */
	public function countProductOrders(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collProductOrders === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ProductOrderPeer::PRODUCT_ID, $this->id);

				$count = ProductOrderPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ProductOrderPeer::PRODUCT_ID, $this->id);

				if (!isset($this->lastProductOrderCriteria) || !$this->lastProductOrderCriteria->equals($criteria)) {
					$count = ProductOrderPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collProductOrders);
				}
			} else {
				$count = count($this->collProductOrders);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ProductOrder object to this object
	 * through the ProductOrder foreign key attribute.
	 *
	 * @param      ProductOrder $l ProductOrder
	 * @return     void
	 * @throws     PropelException
	 */
	public function addProductOrder(ProductOrder $l)
	{
		if ($this->collProductOrders === null) {
			$this->initProductOrders();
		}
		if (!in_array($l, $this->collProductOrders, true)) { // only add it if the **same** object is not already associated
			array_push($this->collProductOrders, $l);
			$l->setProduct($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Product is new, it will return
	 * an empty collection; or if this Product has previously
	 * been saved, it will retrieve related ProductOrders from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Product.
	 */
	public function getProductOrdersJoinCompany($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collProductOrders === null) {
			if ($this->isNew()) {
				$this->collProductOrders = array();
			} else {

				$criteria->add(ProductOrderPeer::PRODUCT_ID, $this->id);

				$this->collProductOrders = ProductOrderPeer::doSelectJoinCompany($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ProductOrderPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastProductOrderCriteria) || !$this->lastProductOrderCriteria->equals($criteria)) {
				$this->collProductOrders = ProductOrderPeer::doSelectJoinCompany($criteria, $con, $join_behavior);
			}
		}
		$this->lastProductOrderCriteria = $criteria;

		return $this->collProductOrders;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Product is new, it will return
	 * an empty collection; or if this Product has previously
	 * been saved, it will retrieve related ProductOrders from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Product.
	 */
	public function getProductOrdersJoinAgentCompany($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collProductOrders === null) {
			if ($this->isNew()) {
				$this->collProductOrders = array();
			} else {

				$criteria->add(ProductOrderPeer::PRODUCT_ID, $this->id);

				$this->collProductOrders = ProductOrderPeer::doSelectJoinAgentCompany($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ProductOrderPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastProductOrderCriteria) || !$this->lastProductOrderCriteria->equals($criteria)) {
				$this->collProductOrders = ProductOrderPeer::doSelectJoinAgentCompany($criteria, $con, $join_behavior);
			}
		}
		$this->lastProductOrderCriteria = $criteria;

		return $this->collProductOrders;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collAgentProducts) {
				foreach ((array) $this->collAgentProducts as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCustomerOrders) {
				foreach ((array) $this->collCustomerOrders as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCustomerProducts) {
				foreach ((array) $this->collCustomerProducts as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collEmployeeProducts) {
				foreach ((array) $this->collEmployeeProducts as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collProductOrders) {
				foreach ((array) $this->collProductOrders as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collAgentProducts = null;
		$this->collCustomerOrders = null;
		$this->collCustomerProducts = null;
		$this->collEmployeeProducts = null;
		$this->collProductOrders = null;
			$this->aEnableCountry = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseProduct:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseProduct::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseProduct
